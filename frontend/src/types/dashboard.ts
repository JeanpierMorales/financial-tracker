export interface Summary { income: number; expenses: number; balance: number; dailyAverage: number; expensePercentage: number }
export interface CategorySummary { category: string; amount: number; percentage: number }
export interface Evolution { month: string; income: number; expenses: number; balance: number }
